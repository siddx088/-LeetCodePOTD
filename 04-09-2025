class Solution {
public:
    int findClosest(int x, int y, int z) {
        int d1 = abs(z - x);  // Distance Person 1 → Person 3
        int d2 = abs(z - y);  // Distance Person 2 → Person 3

        if (d1 < d2) return 1;
        else if (d2 < d1) return 2;
        else return 0;
    }
};
